const chai = require('chai');
const mocha = require('mocha');
const Success = require('../dist/lib/success');

mocha.describe('Success', () => {

    mocha.describe('.filter()', () => {

        mocha.it('should return Success when predicate matches', () => {
            new
            chai.expect()
        });

    });
});